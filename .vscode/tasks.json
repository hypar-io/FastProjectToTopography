{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "server-build",
      "command": "dotnet",
      "type": "process",
      "args": [
        "build",
        "${workspaceFolder}/server/FastProjectToTopography.Server.csproj"
      ],
      "problemMatcher": "$msCompile"
    }
  ]
}